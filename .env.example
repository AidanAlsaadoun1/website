# Optional. Fine-grained PAT with `public_repo` read scope.
# If unset, the projects section falls back to a static snapshot.
GITHUB_TOKEN=

# Public site URL, used for sitemap and OG images.
NEXT_PUBLIC_SITE_URL=https://dev-aidan.com
