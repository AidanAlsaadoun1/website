/**
 * DEPRECATED, kept only because the dev environment couldn't delete it.
 * The contact form was removed; /contact now just surfaces LinkedIn + GitHub.
 *
 * Safe to `rm` this file (and the whole `app/api/contact/` directory) locally.
 */
export {}
